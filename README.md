# Validus testbounty 001

Throwaway repo for testing the Validus Franklin plugin against a real
Base Sepolia USDC payout. Bounties live in `bounties.json`.

## Test bounty scope

This repository exists to exercise the end-to-end bounty flow with a tiny
documentation-only change. The current test bounty accepts a pull request that
updates this README and references `closes #1`.
